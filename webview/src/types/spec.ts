export interface SpecJson {
  specId: string;
  title: string;
  version: string;
}

export interface SpecFile {
  specId: string;
  title: string;
  version: string;
  mdPath: string;
  created: string;
  updated: string;
}
